CREATE TABLE IF NOT EXISTS test_connection_log (
                                                   id BIGINT AUTO_INCREMENT PRIMARY KEY,
                                                   message VARCHAR(255)
    );
